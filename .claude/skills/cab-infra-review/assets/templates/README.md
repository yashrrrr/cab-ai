Drop the approved Module 1 reference templates here (one subfolder per
validation area, e.g. `architecture-review/`, `dpia/`) when they're
provided. The skill should diff/compare uploaded docs against whatever's in
here once populated.
